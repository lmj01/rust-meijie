
use mjalgorithm::mjsort;

fn main() {

    // println!("get5: {:?}", mjsort::uks::get5());

    // let mut numbers = [4, 65, 2, -31, 0, 99, 2, 83, 782, 1];
    // println!("Before: {:?}", numbers);

    // mjsort::bubble::bubble_sort(&mut numbers);
    // println!("After: {:?}\n", numbers);

    mjsort::bubble::bubble_test();
}